const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal,.image-reveal').forEach(el=>observer.observe(el));
const cursor=document.querySelector('.cursor');if(cursor)document.addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});
document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.product').forEach(p=>p.classList.toggle('hidden',f!=='all'&&p.dataset.category!==f))}));
const viewer=document.querySelector('#viewer360');if(viewer){const img=viewer.querySelector('img'),counter=viewer.querySelector('.viewer-hint span'),total=Number(viewer.dataset.frames),path=viewer.dataset.path;let frame=1,startX=0,startFrame=1,drag=false;const src=n=>path.replace('{n}',String(n).padStart(2,'0'));for(let i=1;i<=total;i++){const preload=new Image;preload.src=src(i)}const render=n=>{frame=((n-1+total)%total)+1;img.src=src(frame);counter.textContent=String(frame).padStart(2,'0')};viewer.addEventListener('pointerdown',e=>{drag=true;startX=e.clientX;startFrame=frame;viewer.setPointerCapture(e.pointerId)});viewer.addEventListener('pointermove',e=>{if(!drag)return;render(startFrame-Math.round((e.clientX-startX)/10))});viewer.addEventListener('pointerup',()=>drag=false);viewer.addEventListener('pointercancel',()=>drag=false)}
